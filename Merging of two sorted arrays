#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int n,m;
    scanf("%d", &n);
    int arr1[n];

    for (int i = 0; i < n; i++)
        scanf("%d", &arr1[i]);
         scanf("%d", &m);
    int arr2[m];

    for (int i = 0; i < m; i++)
        scanf("%d", &arr2[i]);
 int c[n+m];
  int i=0,j=0,k=0;
while(i<n && j<m){
    if(arr1[i]<arr2[j])
    c[k++]=arr1[i++];
    else 
    c[k++]=arr2[j++];
}
while(i<n)
    c[k++]=arr1[i++];
while(j<m)
c[k++]=arr2[j++];
 for (i = 0; i < n + m; i++)
        printf("%d ", c[i]);
    
    return 0;
}
